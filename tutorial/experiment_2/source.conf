################################################################################
#                    SOURCE TERM DEFINITION
################################################################################
# Defines the location, height, and emission characteristics of sources.
# Multiple sources can be specified (one per line).
#
# Format: LATITUDE LONGITUDE HEIGHT
# Units: degrees, degrees, meters
################################################################################

# ==============================================================================
# SOURCE LOCATION(S)
# ==============================================================================
# Each line defines one source in the format:
#   LATITUDE  LONGITUDE  HEIGHT(m)
#
# Example:
#   35.71  129.48  100.0    <- Source at 35.71°N, 129.48°E, 100m elevation
#
# Current: Empire State Building, New York City, USA
35.4105 126.4175 100.0

# ==============================================================================
# TRUE EMISSIONS (Reference/Validation)
# ==============================================================================
# The "true" emission time series used for reference simulation
# In real-world applications, this is unknown (what we're trying to find)
# In synthetic tests, this is known and used to validate EKI performance
#
# Units: Becquerels (Bq) per time interval
# Format: One value per line, representing emission rate at each time step
#
# Number of values should match: simulation_duration / time_interval
# Example: 6 hours / 15 minutes = 24 time steps
#
# Pattern design tips:
#   - Constant: easiest for EKI convergence
#   - Single peak: medium difficulty
#   - Multiple peaks: challenging
#   - Late peak: hardest (limited downwind observations)
#
# Current pattern: Three sharp peaks (challenging scenario)
# Peak pattern resembles three vertical bars
TRUE_EMISSION_SERIES=
5.0e+9
5.0e+9
5.0e+9
5.0e+9
8.3e+11
8.3e+11
5.0e+9
5.0e+9
5.0e+9
5.0e+9
5.0e+9
5.0e+9
9.0e+11
9.0e+11
5.0e+9
5.0e+9
5.0e+9
5.0e+9
5.0e+9
8.8e+11
8.8e+11
5.0e+9
5.0e+9
5.0e+9

# ==============================================================================
# PRIOR EMISSIONS (Initial Guess)
# ==============================================================================
# The initial guess for emission time series before EKI optimization
# EKI will iteratively refine this guess to match receptor observations
#
# Two modes available:

# Mode selection: "constant" or "series"
# - constant: All time steps use same emission rate (simpler, recommended)
# - series: Specify different rate for each time step (more flexible)
PRIOR_EMISSION_MODE= constant

# Constant mode: single emission rate for all time steps (Bq)
# This value is used when PRIOR_EMISSION_MODE = constant
# Recommended: Use order-of-magnitude estimate from preliminary analysis
# Example: 1.0e+12 Bq = 1 TBq (typical for regional nuclear release)
#
# Rule of thumb: Set 1-2 orders of magnitude below expected release
# Example: If truth is ~1.0e+12 Bq/s, use prior of 1.0e+10 to 1.0e+11
# Too high: Poor convergence; Too low: Excessive iterations
PRIOR_EMISSION_CONSTANT= 1.0e+10

# Series mode: individual emission rates per time step (Bq)
# Uncomment and fill in when PRIOR_EMISSION_MODE = series
# Must have same number of values as TRUE_EMISSION_SERIES
# PRIOR_EMISSION_SERIES=
# 1.5e+8
# 1.5e+8
# ... (24 values for 6-hour simulation with 15-min intervals)

# ==============================================================================
# GRID CONFIGURATION [DISABLED IN v1.0]
# ==============================================================================
#
# ⚠️  NOT USED IN v1.0 - SECTION DISABLED ⚠️
#
# The GRID_CONFIG section below is NOT read or used by the LDM-EKI system.
# The loadGridConfig() function has been modified to return default values
# without parsing this section to avoid confusion.
#
# These parameters are kept as reference for future versions but have NO EFFECT
# on the current simulation. The values below are IGNORED.
#
# For v1.0, only particle-based receptor observations are used.
# See receptor.conf for active measurement locations.
#
# ==============================================================================

# [GRID_CONFIG] -- COMMENTED OUT, NOT USED
# # Latitude range (degrees North)
# start_lat: 35.0
# end_lat: 37.0
#
# # Longitude range (degrees East)
# start_lon: 129.0
# end_lon: 131.0
#
# # Grid spacing (degrees)
# lat_step: 0.5
# lon_step: 0.5

################################################################################
# End of source.conf
################################################################################
